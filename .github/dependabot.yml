version: 2
updates:
  - package-ecosystem: "github-actions"  # 只监控 GitHub Actions
    directory: "/"                       # workflow 文件所在根目录
    schedule:
      interval: "daily"                  # 每天检查一次更新
